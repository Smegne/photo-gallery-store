<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Photo Details</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
</head>
<body class="bg-light py-5">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-lg-10">
        <div class="card shadow-sm">
          <div class="card-body row">
            <div class="col-md-8 mb-3">
              <img src="<%= photo.image_path %>" class="img-fluid rounded" alt="<%= photo.name %>" />
            </div>
            <div class="col-md-4">
              <h2 class="fw-bold"><%= photo.name %></h2>
              <p class="text-muted mb-1"><strong>Category:</strong> <%= photo.category || 'N/A' %></p>
              <p class="text-muted"><%= photo.description || 'No description available.' %></p>
              <a href="mailto:info@photogallery.com?subject=Purchase%20Inquiry%20for%20<%= encodeURIComponent(photo.name) %>" class="btn btn-primary mt-3">Contact for Purchase</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
